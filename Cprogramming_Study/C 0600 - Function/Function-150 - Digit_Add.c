#include <stdio.h>

int digit_Add(int num){
    int digit_sum = 0;

    //Answer

    return digit_sum;
}

int main(void)
{
	int num;

	scanf("%d", &num);
    printf("Digit Sum : %d",digit_Add(num));
	return 0;
}
