#include <stdio.h>
/* Get and return the GCD of integer a and b */
int GCD(int a, int b) {
    //your code

}

int main(void) {
    int a, b;
    printf("Enter two positive integers\n");
    scanf("%d%d",&a,&b);
    printf("GCD of (%d,%d) is %d\n",a,b,GCD(a,b));
    return 0;
}
